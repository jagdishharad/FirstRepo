apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychart_jag.fullname" . }}
  labels:
    {{- include "mychart_jag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mychart_jag.selectorLabels" . | nindent 4 }}
